<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <button id="close_delete_board_modal" type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h4 class="modal-title" id="delete_board_modal_label">Delete board</h4>
    </div>
            <%= form_for(@board, url: board_path(@board.id),:html => {:class => 'form-horizontal'}, method: :delete) do |f| %>

    <div class="modal-body">
        Are you sure you want to delete this board: <%= @board.title%>, and all of its tasks?  This is a permenant change, it cannot be undone.
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
          <%= f.submit 'Delete board', class: 'btn btn-primary' %>
    </div>
    <% end %>
  </div>
</div>
