<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <button id="close_edit_task_modal" type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h4 class="modal-title" id="edit_task_modal_label">Edit Task</h4>
    </div>
            <%= form_for(@task, url: board_task_path(@task.board.id, @task.id),:html => {:class => 'form-horizontal'}, method: :put) do |f| %>

    <div class="modal-body">
        <%= render(partial: 'form', locals: {f: f}) %>
        <div class="form-group">
  <%= f.label(:completed_date,  class: "col-sm-2 control-label") %>
        <div class="col-sm-10">
<%= f.date_select(:completed_date, {start_year: Date.today.year, include_blank: true, order: [:month, :day, :year]}, {class: 'form-control date_select'}) %>
</div></div>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
          <%= f.submit 'Save Changes', class: 'btn btn-primary' %>
    </div>
    <% end %>
  </div>
</div>
