<%= link_to('<< Back to Users', {controller: 'users', action: 'index'}) %>

<h1>Create New User</h1>

<%= form_for(:user, url: {action: 'create'}) do |f| %>
<%= render(partial: 'form', locals: {f: f}) %>

    <%= f.submit 'Create User', class: 'btn btn-default' %>
  <%end%>